{
  "name": "sqlprism",
  "version": "0.1.0",
  "description": "The universal SQL refraction engine for the modern web.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "demo:osi": "npm run build && node demos/osi-demo.js",
    "demo:vega": "npm run build && node demos/vega-demo.js",
    "demo:all": "npm run build && node demos/build-showcase-data.js && node demos/osi-demo.js && node demos/vega-demo.js"
  },
  "keywords": [
    "sql",
    "parser",
    "vega-lite",
    "duckdb",
    "semantic-layer"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.1.0"
  }
}
